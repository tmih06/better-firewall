# /etc/better-firewall/sysctl.conf
#
# Kernel network tunables applied when better-firewall is enabled.
# Uncomment to enable forwarding (required for `bfw route` rules and NAT).

#net/ipv4/ip_forward=1
#net/ipv6/conf/default/forwarding=1
#net/ipv6/conf/all/forwarding=1
